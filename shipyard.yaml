apiVersion: "spec.keptn.sh/0.2.0"
kind: Shipyard
metadata:
  name: "shipyard-echo-service"
spec:
  stages:
    - name: "stageone"
      sequences:
        - name: "echosequence"
          tasks:
            - name: "echo"
